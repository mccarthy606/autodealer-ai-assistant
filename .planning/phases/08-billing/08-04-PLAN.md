---
plan: 08-04
phase: 08-billing
title: "Billing tests — is_subscription_active(), webhook_lemon events, access gate"
wave: 3
req_ids: BILL-01, BILL-02, BILL-03, BILL-04
status: complete
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - tests/conftest.py
  - tests/test_billing.py
autonomous: true
must_haves:
  truths:
    - "is_subscription_active() returns correct boolean for all 7 subscription states plus None"
    - "Naive datetime from DB does not raise TypeError when compared in is_subscription_active()"
    - "webhook_lemon subscription_created correctly writes all 5 fields to the dealership row"
    - "webhook_lemon subscription_payment_failed uses data.attributes.subscription_id (not data.id)"
    - "webhook_lemon subscription_payment_failed sets grace_period_ends_at to approximately now+7d"
    - "webhook_lemon missing custom_data.dealership_id returns 200 without raising"
    - "webhook_lemon unknown dealership_id returns 200 without raising"
    - "webhook_cloud drops inbound message (returns ok) when subscription is inactive"
    - "webhook_cloud processes message normally when subscription is active"
    - "followup_task increments skipped counter and does not call WhatsApp API for inactive subscription"
  artifacts:
    - path: "tests/conftest.py"
      provides: "5 new billing fixtures: active_dealership, trial_dealership, past_due_in_grace_dealership, expired_dealership, no_subscription_dealership"
      contains: "subscription_status, grace_period_ends_at, trial_ends_at"
    - path: "tests/test_billing.py"
      provides: "Full test coverage for billing service, webhook handler, and access gates"
      contains: "test_is_subscription_active, test_webhook_lemon, test_subscription_gate"
  key_links:
    - from: "tests/test_billing.py"
      to: "src/services/billing.py is_subscription_active()"
      via: "direct import and call with fixture dealerships"
    - from: "tests/test_billing.py webhook tests"
      to: "src/api/routes/webhook_lemon.py"
      via: "AsyncClient POST to /webhooks/lemon-squeezy with HMAC-signed payloads"
    - from: "tests/test_billing.py gate tests"
      to: "src/api/routes/webhook_cloud.py"
      via: "AsyncClient POST to /webhooks/whatsapp_cloud with mock dealership subscriptions"
---

<objective>
Add billing fixtures to conftest.py and create tests/test_billing.py covering the is_subscription_active() logic, webhook_lemon event handling, and the subscription gate in webhook_cloud.

Purpose: Verifies the entire Phase 8 integration. Catches the naive datetime hazard and the payment_failed invoice-vs-subscription-id confusion before they reach production.
Output: pytest suite that passes green; 100% coverage of billing decision logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-billing/08-CONTEXT.md
@.planning/phases/08-billing/08-RESEARCH.md
@tests/conftest.py
</context>

<interfaces>
<!-- Existing conftest.py structure -->
- Fixtures use @pytest_asyncio.fixture + async def + db_session: AsyncSession parameter
- Dealership created with db_session.add(d) + await db_session.flush()
- _DEALER1_HASH is module-level computed bcrypt hash — reuse for billing fixtures
- Existing fixture ids: 1 (dealership), 2 (dealership2) — billing fixtures use 10-14 to avoid collision

<!-- New fixtures to add (from RESEARCH.md Q6) -->
active_dealership (id=10): subscription_status="active", subscription_id="sub_active_001", ls_customer_id="cust_001", plan="basic"
trial_dealership (id=11): subscription_status="trial", subscription_id="sub_trial_001", ls_customer_id="cust_002", plan="basic", trial_ends_at=datetime.now(UTC)+timedelta(days=5)
past_due_in_grace_dealership (id=12): subscription_status="past_due", subscription_id="sub_grace_001", ls_customer_id="cust_003", grace_period_ends_at=datetime.now(UTC)+timedelta(days=3)
expired_dealership (id=13): subscription_status="expired", subscription_id="sub_expired_001", ls_customer_id="cust_004"
no_subscription_dealership (id=14): all subscription fields None

<!-- HMAC signing helper for webhook tests -->
import hashlib, hmac, json
def _sign(payload_bytes: bytes, secret: str) -> str:
    return hmac.new(secret.encode(), payload_bytes, hashlib.sha256).hexdigest()

<!-- Minimal LS payload builders -->
subscription_created payload:
  meta.event_name = "subscription_created"
  meta.custom_data.dealership_id = str(dealership.id)
  data.type = "subscriptions"
  data.id = "sub_999"
  data.attributes.status = "on_trial"
  data.attributes.customer_id = 555
  data.attributes.variant_name = "basic"
  data.attributes.trial_ends_at = "2026-04-03T13:43:48.000000Z"

subscription_payment_failed payload (CRITICAL: different data.type):
  meta.event_name = "subscription_payment_failed"
  meta.custom_data.dealership_id = str(dealership.id)
  data.type = "subscription_invoices"
  data.id = "invoice_888"           ← invoice ID, NOT subscription ID
  data.attributes.subscription_id = 999  ← integer — this is the real subscription ID
  data.attributes.customer_id = 555
  data.attributes.status = "failed"

<!-- App fixture pattern for route tests -->
Use httpx.AsyncClient with app from src.main (or create a minimal app fixture).
Override get_db dependency to inject the test db_session.
Pattern already established in other test files in this project.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 5 billing fixtures to conftest.py</name>
  <files>tests/conftest.py</files>
  <behavior>
    - active_dealership fixture: id=10, subscription_status="active", subscription_id, ls_customer_id, plan fields set
    - trial_dealership fixture: id=11, subscription_status="trial", trial_ends_at = now+5 days (aware UTC)
    - past_due_in_grace_dealership fixture: id=12, subscription_status="past_due", grace_period_ends_at = now+3 days (aware UTC)
    - expired_dealership fixture: id=13, subscription_status="expired", no dates set
    - no_subscription_dealership fixture: id=14, all subscription fields absent (None by default)
    - All fixtures use @pytest_asyncio.fixture, async def, db_session parameter, db_session.add() + await db_session.flush()
    - All fixtures reuse _DEALER1_HASH for admin_password_hash (already computed)
    - Fixture IDs 10-14 do not collide with existing fixtures (1, 2)
  </behavior>
  <action>
    Append the 5 new fixtures to the end of tests/conftest.py. Do not modify any existing fixture or import.

    Add this import if not already present (it is not in current conftest.py):
      from datetime import datetime, UTC, timedelta

    Add each fixture in this order:
    1. active_dealership (id=10) — active subscription, no date fields needed
    2. trial_dealership (id=11) — trial status, trial_ends_at = datetime.now(UTC) + timedelta(days=5)
    3. past_due_in_grace_dealership (id=12) — past_due status, grace_period_ends_at = datetime.now(UTC) + timedelta(days=3)
    4. expired_dealership (id=13) — expired status, no date fields
    5. no_subscription_dealership (id=14) — no subscription fields (all None)

    All 5 fixtures must also set:
    - A unique whatsapp_phone_number_id (use "3333333333" through "7777777777")
    - A unique admin_username
    - admin_password_hash = _DEALER1_HASH
    - name field (required, nullable=False)

    The trial_ends_at and grace_period_ends_at values should be stored as timezone-aware
    datetime objects (datetime.now(UTC) + timedelta(...)). SQLAlchemy will strip tzinfo
    when writing to SQLite test DB, which is intentional — the billing.py normalization
    code is designed to handle this exact round-trip.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -m pytest tests/conftest.py --collect-only -q 2>&1 | head -20 && python -c "
import ast
with open('tests/conftest.py') as f:
    src = f.read()
ast.parse(src)
for name in ['active_dealership', 'trial_dealership', 'past_due_in_grace_dealership', 'expired_dealership', 'no_subscription_dealership']:
    assert name in src, f'Missing fixture: {name}'
print('all 5 billing fixtures found in conftest.py')
"</automated>
  </verify>
  <done>conftest.py parses without error. All 5 fixture names present. Existing fixtures unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_billing.py — full billing coverage</name>
  <files>tests/test_billing.py</files>
  <behavior>
    is_subscription_active() unit tests (no DB needed, use simple object stubs):
    - test_active_returns_true: status="active" → True
    - test_trial_returns_true: status="trial" → True
    - test_past_due_in_grace_returns_true: status="past_due", grace=now+3d → True
    - test_past_due_grace_expired_returns_false: status="past_due", grace=now-1d → False
    - test_past_due_no_grace_returns_false: status="past_due", grace=None → False
    - test_cancelled_returns_false: status="cancelled" → False
    - test_expired_returns_false: status="expired" → False
    - test_none_status_with_future_trial_returns_true: status=None, trial_ends_at=now+5d → True (D-19 exception)
    - test_none_status_no_trial_returns_false: status=None, trial_ends_at=None → False
    - test_none_dealership_returns_false: is_subscription_active(None) → False
    - test_naive_datetime_no_typeerror: status="past_due", grace=naive future datetime (tzinfo=None) → True, no TypeError raised

    map_ls_status() unit tests:
    - test_on_trial_maps_to_trial
    - test_paused_maps_to_past_due
    - test_unpaid_maps_to_past_due
    - test_unknown_maps_to_expired (safe default)

    webhook_lemon integration tests (use db_session + AsyncClient with app):
    - test_webhook_subscription_created_writes_fields: POST valid signed payload for subscription_created with on_trial status; assert dealership row has subscription_id="sub_999", subscription_status="trial", ls_customer_id="555", plan="basic", trial_ends_at is not None
    - test_webhook_subscription_payment_failed_uses_attrs_subscription_id: POST signed payment_failed payload where data.id="invoice_888" and data.attributes.subscription_id=999; assert dealership.subscription_status="past_due", dealership.grace_period_ends_at is not None (approx now+7d), dealership.subscription_id="999"
    - test_webhook_subscription_cancelled: POST signed cancelled payload; assert dealership.subscription_status="cancelled"
    - test_webhook_subscription_expired_clears_grace: Set dealership.grace_period_ends_at=now+3d first; POST signed expired payload; assert status="expired" AND grace_period_ends_at is None
    - test_webhook_missing_custom_data_returns_200: POST payload without meta.custom_data; assert response 200 {"status": "ok"}, no exception
    - test_webhook_unknown_dealership_returns_200: POST payload with custom_data.dealership_id="99999"; assert response 200 {"status": "ok"}, no exception
    - test_webhook_invalid_signature_returns_401: POST with wrong X-Signature; assert response 401

    webhook_cloud subscription gate tests:
    - test_gate_inactive_subscription_drops_message: mock get_dealership_by_wa to return expired_dealership; POST WA message; assert process_message NOT called, response {"status": "ok"}
    - test_gate_active_subscription_processes_message: mock get_dealership_by_wa to return active_dealership; POST WA message; assert process_message IS called

    followup_task subscription gate test:
    - test_followup_skips_inactive_dealership: Create conversation for expired_dealership in eligible state; call send_followups(); assert WhatsApp API not called for that conversation, skipped count incremented
  </behavior>
  <action>
    Create tests/test_billing.py.

    File structure:
    1. Imports: pytest, pytest_asyncio, httpx.AsyncClient, datetime, UTC, timedelta, json, hmac, hashlib, unittest.mock
    2. Import billing service: from src.services.billing import is_subscription_active, map_ls_status
    3. Simple stub class for unit tests (no DB):
         class StubDealer:
             def __init__(self, **kw):
                 self.subscription_status = kw.get("subscription_status")
                 self.grace_period_ends_at = kw.get("grace_period_ends_at")
                 self.trial_ends_at = kw.get("trial_ends_at")

    4. Unit test group — test_is_subscription_active (11 tests, no fixtures, no async):
       Use StubDealer. All inputs and expected outputs match the behavior block above.
       For test_naive_datetime_no_typeerror: create naive datetime with .replace(tzinfo=None)
       and confirm no TypeError is raised (the fix in billing.py handles this).

    5. Unit test group — test_map_ls_status (4 tests):
       Direct calls to map_ls_status() with various inputs.

    6. Integration test group — test_webhook_lemon (7 tests, async, use dealership fixture + db_session):
       For each test, build a minimal LS payload dict, serialize to JSON bytes, compute
       HMAC-SHA256 signature using settings.lemon_squeezy_webhook_secret (or a test secret
       if that config is not set in test env — override settings if needed):

         test_secret = "test-webhook-secret"
         payload_bytes = json.dumps(payload).encode()
         signature = hmac.new(test_secret.encode(), payload_bytes, hashlib.sha256).hexdigest()

       Use override of settings.lemon_squeezy_webhook_secret = test_secret before test runs.
       POST to /webhooks/lemon-squeezy with header {"x-signature": signature}.

       For subscription_created test: verify all 5 fields by reloading from db_session after
       response (use await db_session.refresh(dealer) or re-query).

       For payment_failed test: build payload where data.id="invoice_888" (invoice) and
       data.attributes.subscription_id=999 (int). Assert dealer.subscription_id="999" and
       dealer.grace_period_ends_at is approximately now+7d (within ±60 seconds).

    7. Integration test group — test_subscription_gate (2 tests, async, mock):
       Use unittest.mock.patch("src.api.routes.webhook_cloud.get_dealership_by_wa") to
       return active_dealership or expired_dealership fixture.
       Use unittest.mock.patch("src.api.routes.webhook_cloud.process_message") to track
       whether it was called.
       Build a minimal WhatsApp Cloud webhook payload with a valid message entry.

    8. followup_task gate test (1 test, sync):
       Create a Conversation row for expired_dealership.id in PRESENTING stage with
       last_message_at = now - 48h (eligible for followup_1).
       Patch WhatsAppCloudAdapter.send_template to a MagicMock.
       Call send_followups() directly.
       Assert send_template was NOT called for the expired dealership's conversation.
       Assert result["skipped"] >= 1.

    Use monkeypatch or pytest.ini conftest overrides to set LEMON_SQUEEZY_WEBHOOK_SECRET
    for the webhook tests. Simplest approach: monkeypatch.setattr(settings, "lemon_squeezy_webhook_secret", "test-webhook-secret").

    Do NOT create a second DB engine — reuse db_session fixture from conftest.py.
    For async route tests, use the standard httpx.AsyncClient(app=app, base_url="http://test")
    pattern with app-level get_db override to inject db_session.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -m pytest tests/test_billing.py -x -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>All tests in test_billing.py pass. No existing tests regressed. Test count: at minimum 11 (is_subscription_active) + 4 (map_ls_status) + 7 (webhook_lemon) + 2 (webhook_cloud gate) + 1 (followup gate) = 25 tests. pytest exits with code 0.</done>
</task>

</tasks>

<verification>
cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -20
Full suite must be green. test_billing.py provides the regression guard for the naive datetime hazard and the payment_failed invoice-id confusion — both would be silent production bugs without these tests.
</verification>

<success_criteria>
- tests/conftest.py has 5 new billing fixtures (ids 10-14) without breaking existing fixtures
- tests/test_billing.py exists with at least 25 test functions covering all billing decision branches
- The naive datetime normalization test explicitly confirms no TypeError
- The payment_failed test explicitly uses data.id="invoice_888" (wrong) vs data.attributes.subscription_id=999 (correct) to verify the handler reads from the right field
- Full pytest suite green
</success_criteria>

<output>
After completion, create .planning/phases/08-billing/08-04-SUMMARY.md with:
- Final test count and pass rate
- Any test patterns that differed from the plan (e.g., app fixture approach used)
- Coverage of the naive datetime and payment_failed edge cases
</output>
