---
plan: 08-03
phase: 08-billing
title: "Subscription gate in webhook_cloud.py + followup_task.py + admin settings badge"
wave: 2
req_ids: BILL-03, BILL-04
status: complete
depends_on: [08-01, 08-02]
files_modified:
  - src/api/routes/webhook_cloud.py
  - src/tasks/followup_task.py
  - src/api/routes/admin_settings.py
  - src/templates/admin/settings.html
autonomous: true
must_haves:
  truths:
    - "WhatsApp messages from dealerships with inactive subscriptions are silently dropped (200 OK, no reply)"
    - "WhatsApp messages from dealerships with active/trial/grace-period subscriptions are processed normally"
    - "Follow-up task skips dealerships with inactive subscriptions (no WhatsApp API call made)"
    - "Admin settings page displays subscription status badge for all subscription states including no-subscription"
    - "Admin settings.html GET handler needs no Python changes — dealer object already passed"
  artifacts:
    - path: "src/api/routes/webhook_cloud.py"
      provides: "Subscription gate after dealership lookup, before process_message()"
      contains: "is_subscription_active"
    - path: "src/tasks/followup_task.py"
      provides: "Subscription gate after dealer = session.get(Dealership, ...) at line ~181"
      contains: "is_subscription_active"
    - path: "src/templates/admin/settings.html"
      provides: "Third card block showing subscription status, plan, trial_ends_at, grace_period_ends_at"
      contains: "dealer.subscription_status"
  key_links:
    - from: "src/api/routes/webhook_cloud.py receive_whatsapp_message()"
      to: "src/services/billing.py is_subscription_active()"
      via: "import and call immediately after 'if dealer is None: return' block (line ~80)"
    - from: "src/tasks/followup_task.py send_followups()"
      to: "src/services/billing.py is_subscription_active()"
      via: "import and call after 'dealer = session.get(Dealership, conv.dealership_id)' at line ~181"
    - from: "src/templates/admin/settings.html"
      to: "src/db/models.py Dealership"
      via: "dealer.subscription_status, dealer.plan, dealer.trial_ends_at, dealer.grace_period_ends_at accessed via template context"
---

<objective>
Wire the is_subscription_active() helper into the two message-processing paths (WhatsApp webhook and follow-up Celery task) and add a read-only subscription status card to the admin settings page.

Purpose: Makes billing gating visible to users and operational. Without this plan, the DB columns and billing.py from 08-01 exist but are never consulted during message processing.
Output: Inbound WhatsApp processing blocked for inactive subs. Follow-up sends skipped for inactive subs. Admin page shows subscription state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-billing/08-CONTEXT.md
@src/api/routes/webhook_cloud.py
@src/tasks/followup_task.py
@src/api/routes/admin_settings.py
@src/templates/admin/settings.html
</context>

<interfaces>
<!-- webhook_cloud.py insertion point (per CONTEXT.md code_context, D-15) -->
Current code around line 74-82:
  dealer = None
  if phone_number_id:
      dealer = await get_dealership_by_wa(db, phone_number_id)
  if dealer is None:
      logger.info("No dealership for phone_number_id=%s, ignoring", phone_number_id)
      return {"status": "ok"}
  # INSERT GATE HERE (after this block, before dealership_id = dealer.id)
  dealership_id = dealer.id

Insert after the "if dealer is None" return block:
  if not is_subscription_active(dealer):
      logger.info("Subscription inactive for dealership=%d, dropping message", dealer.id)
      return {"status": "ok"}

New import: from src.services.billing import is_subscription_active

<!-- followup_task.py insertion point (per RESEARCH.md Q5, D-20) -->
Current code around line 180-184:
  dealer = session.get(Dealership, conv.dealership_id)
  wa_phone_id = (dealer.whatsapp_phone_number_id if dealer else None) or settings.whatsapp_phone_number_id
  wa_token = (dealer.whatsapp_access_token if dealer else None) or settings.whatsapp_cloud_token

Insert BETWEEN dealer load and wa_phone_id assignment:
  if not is_subscription_active(dealer):
      skipped += 1
      continue

New import: from src.services.billing import is_subscription_active
Note: is_subscription_active(None) returns False — handles orphaned conv where session.get() returns None

<!-- settings.html insertion point (per RESEARCH.md Q4) -->
Current structure (lines 40-60):
  </div>  ← closes "Bot behavior" card
  <div class="form-actions">  ← line 57

Insert a new <div class="card"> block between these two.
The GET route (admin_settings.py) already passes dealer — no Python changes needed.
Existing CSS classes to use: text-success, text-warning, text-muted, info-list, info-item
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add subscription gate to webhook_cloud.py and followup_task.py</name>
  <files>src/api/routes/webhook_cloud.py, src/tasks/followup_task.py</files>
  <action>
    WEBHOOK_CLOUD.PY:

    1. Add import at top of file (with other src.services imports or after existing imports):
         from src.services.billing import is_subscription_active

    2. Find the block that ends with:
         if dealer is None:
             logger.info("No dealership for phone_number_id=%s, ignoring", phone_number_id)
             return {"status": "ok"}

       Immediately after that return block, before the line:
         dealership_id = dealer.id

       Insert (per D-15, D-16):
         if not is_subscription_active(dealer):
             logger.info(
                 "Subscription inactive for dealership=%d (status=%s), dropping WA message",
                 dealer.id,
                 dealer.subscription_status,
             )
             return {"status": "ok"}

    No other changes to webhook_cloud.py. The check is a silent 200 drop — no error response,
    no customer-facing message (per D-15).

    ---

    FOLLOWUP_TASK.PY:

    1. Add import at top of file (with other src imports):
         from src.services.billing import is_subscription_active

    2. Find the line (around line 181):
         dealer = session.get(Dealership, conv.dealership_id)

       The very next line is:
         wa_phone_id = (dealer.whatsapp_phone_number_id if dealer else None) or settings.whatsapp_phone_number_id

       Insert between these two lines (per D-20, RESEARCH.md Q5):
         if not is_subscription_active(dealer):
             skipped += 1
             continue

    Why here and not in _should_followup(): the dealership is already loaded at line 181
    so no extra DB query. is_subscription_active(None) returns False, covering orphaned
    conversations where session.get() returns None (per RESEARCH.md Q5 "None situation").

    No other changes to followup_task.py.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -c "
import ast

for path in ['src/api/routes/webhook_cloud.py', 'src/tasks/followup_task.py']:
    with open(path) as f:
        src = f.read()
    ast.parse(src)
    assert 'is_subscription_active' in src, f'{path}: missing is_subscription_active'
    print(f'{path}: ok')

# Verify insertion order in webhook_cloud.py — gate must come before dealership_id = dealer.id
with open('src/api/routes/webhook_cloud.py') as f:
    src = f.read()
gate_pos = src.index('is_subscription_active(dealer)')
dealership_id_pos = src.index('dealership_id = dealer.id')
assert gate_pos < dealership_id_pos, 'gate must be BEFORE dealership_id = dealer.id'
print('gate ordering correct in webhook_cloud.py')
"</automated>
  </verify>
  <done>Both files parse without error. is_subscription_active imported and called in both. Gate in webhook_cloud.py appears before dealership_id = dealer.id. Gate in followup_task.py appears between dealer = session.get(...) and wa_phone_id assignment. Existing logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add subscription status card to settings.html</name>
  <files>src/templates/admin/settings.html</files>
  <action>
    Edit src/templates/admin/settings.html.

    Find the closing tag of the "Bot behavior" card:
      </div>   ← closes <div class="info-list">
    </div>     ← closes <div class="card"> for "Bot behavior"

    After that closing </div> (which is currently on line 55 of the file), and BEFORE:
      <div class="form-actions">

    Insert this new card block (read-only, no form inputs — per D-21):

    <div class="card">
        <h3>Subscription</h3>
        <div class="info-list">
            <div class="info-item">
                <span>Status</span>
                <strong class="{% if dealer and dealer.subscription_status in ('trial', 'active') %}text-success{% elif dealer and dealer.subscription_status == 'past_due' %}text-warning{% else %}text-muted{% endif %}">
                    {{ dealer.subscription_status | upper if dealer and dealer.subscription_status else 'No subscription' }}
                </strong>
            </div>
            {% if dealer and dealer.plan %}
            <div class="info-item">
                <span>Plan</span>
                <strong>{{ dealer.plan }}</strong>
            </div>
            {% endif %}
            {% if dealer and dealer.trial_ends_at %}
            <div class="info-item">
                <span>Trial ends</span>
                <strong>{{ dealer.trial_ends_at.strftime('%Y-%m-%d') }}</strong>
            </div>
            {% endif %}
            {% if dealer and dealer.subscription_status == 'past_due' and dealer.grace_period_ends_at %}
            <div class="info-item">
                <span>Grace period ends</span>
                <strong class="text-warning">{{ dealer.grace_period_ends_at.strftime('%Y-%m-%d') }}</strong>
            </div>
            {% endif %}
        </div>
    </div>

    CSS class notes (all pre-existing in this codebase — no new CSS needed):
    - text-success: green, used on "LLM enabled = Yes" in the same file
    - text-warning: amber/orange — used in integrations.html
    - text-muted: grey — used in the same file for "No (deterministic mode)"
    - info-list, info-item: used in both settings.html and integrations.html

    Do NOT add a <p class="hint"> paragraph to this card — the subscription data is
    self-explanatory and the "Bot behavior" card pattern with hint text is for env var
    context which does not apply here.

    admin_settings.py GET handler needs NO changes — dealer is already passed to the template
    on line 31 of that file and the new ORM columns will be accessible automatically.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -c "
with open('src/templates/admin/settings.html') as f:
    content = f.read()

required_strings = [
    'Subscription',
    'dealer.subscription_status',
    'text-success',
    'text-warning',
    'text-muted',
    'trial_ends_at',
    'grace_period_ends_at',
    'dealer.plan',
    'No subscription',
]
missing = [s for s in required_strings if s not in content]
if missing:
    print('MISSING in template:', missing)
    exit(1)

# Verify card appears before form-actions
sub_card_pos = content.index('Subscription')
form_actions_pos = content.index('form-actions')
assert sub_card_pos < form_actions_pos, 'Subscription card must appear before form-actions'
print('settings.html: all checks passed')
"</automated>
  </verify>
  <done>settings.html contains the Subscription card with all 9 required strings. The card appears before the form-actions div. Status badge uses text-success/text-warning/text-muted classes matching existing CSS. trial_ends_at and grace_period_ends_at shown conditionally. No new CSS or Python route changes required.</done>
</task>

</tasks>

<verification>
cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -m pytest tests/ -x -q --tb=short 2>&1 | head -40
Also: python -c "from src.api.routes.webhook_cloud import router; from src.tasks.followup_task import send_followups; print('imports ok')"
Confirm no existing tests regressed (the gate returns {"status": "ok"} which is the same value existing tests mock away).
</verification>

<success_criteria>
- webhook_cloud.py: is_subscription_active(dealer) called after the "if dealer is None" guard, before dealership_id = dealer.id; returns {"status": "ok"} silently on inactive sub
- followup_task.py: is_subscription_active(dealer) called after session.get(), before wa_phone_id assignment; increments skipped counter and continues
- settings.html: Subscription card visible between Bot behavior card and Save button; status badge color-coded; plan/trial/grace shown conditionally
- admin_settings.py: no changes (dealer already in context)
- All existing tests pass
</success_criteria>

<output>
After completion, create .planning/phases/08-billing/08-03-SUMMARY.md with:
- Exact line numbers where gates were inserted in webhook_cloud.py and followup_task.py
- Confirmation that admin_settings.py needed no changes
- Screenshot description or template HTML snippet showing subscription card
</output>
