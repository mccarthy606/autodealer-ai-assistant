---
plan: 08-01
phase: 08-billing
title: "Migration 007 + Dealership model columns + billing.py service"
wave: 1
req_ids: BILL-01, BILL-03, BILL-04
status: pending
depends_on: []
files_modified:
  - alembic/versions/007_billing_subscription_columns.py
  - src/db/models.py
  - src/services/billing.py
autonomous: true
must_haves:
  truths:
    - "alembic upgrade head applies migration 007 without error"
    - "Dealership ORM object has all 6 subscription attributes accessible"
    - "is_subscription_active(dealership) returns True for active, trial, and past_due-in-grace dealerships"
    - "is_subscription_active(dealership) returns False for expired, cancelled, no-subscription, and None dealerships"
    - "Naive datetime from DB is normalized before comparison — no TypeError raised"
  artifacts:
    - path: "alembic/versions/007_billing_subscription_columns.py"
      provides: "Alembic migration adding 6 subscription columns to dealerships table"
      contains: "revision = \"007\", down_revision = \"006\""
    - path: "src/db/models.py"
      provides: "Dealership model with 6 new nullable subscription columns"
      contains: "subscription_status, subscription_id, ls_customer_id, plan, trial_ends_at, grace_period_ends_at"
    - path: "src/services/billing.py"
      provides: "Pure Python is_subscription_active() + LS_STATUS_MAP + map_ls_status()"
      exports: ["is_subscription_active", "LS_STATUS_MAP", "map_ls_status"]
  key_links:
    - from: "src/db/models.py Dealership"
      to: "alembic/versions/007_billing_subscription_columns.py"
      via: "Column names must match exactly between ORM definition and migration op.add_column() calls"
    - from: "src/services/billing.py is_subscription_active()"
      to: "src/db/models.py Dealership"
      via: "Reads .subscription_status, .grace_period_ends_at, .trial_ends_at attributes"
---

<objective>
Add the Alembic migration that extends the dealerships table with 6 subscription columns, add matching ORM columns to the Dealership model, and create the pure-Python billing service that gates access.

Purpose: This is the foundation for all billing gating. Every other plan in Phase 8 imports from billing.py or depends on the DB columns being present.
Output: Working migration file, updated Dealership ORM class, and an importable billing service with is_subscription_active().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-billing/08-CONTEXT.md
@.planning/phases/08-billing/08-RESEARCH.md
@src/db/models.py
</context>

<interfaces>
<!-- Existing Dealership model (src/db/models.py lines 86-108) — add columns here -->
class Dealership(Base):
    __tablename__ = "dealerships"
    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(255), nullable=False)
    # ... existing columns ...
    created_at = Column(DateTime, default=_utcnow)
    # 6 new nullable columns go BEFORE the relationships block

<!-- Confirmed migration pattern from 006_multi_tenancy_dealership_columns.py -->
# Column types: String(32), String(128), String(128), String(64), DateTime, DateTime
# All nullable=True, no server_default
# upgrade() uses op.add_column(); downgrade() uses op.drop_column() in reverse order

<!-- billing.py public contract — implement exactly this -->
LS_STATUS_MAP: dict[str, str]  # maps LS status strings to our internal status strings
def map_ls_status(ls_status: str) -> str: ...   # returns "expired" for unknown values
def is_subscription_active(dealership) -> bool: ...  # pure Python, no I/O, no async
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 007 — add 6 subscription columns to dealerships</name>
  <files>alembic/versions/007_billing_subscription_columns.py</files>
  <behavior>
    - upgrade() adds columns in this order: subscription_status String(32), subscription_id String(128), ls_customer_id String(128), plan String(64), trial_ends_at DateTime, grace_period_ends_at DateTime — all nullable=True
    - downgrade() drops them in reverse order
    - revision = "007", down_revision = "006" (per D-03, confirmed from research Q2: chain is 001→002→003→004→006→007, no 005)
    - No server_default on any column — existing rows get NULL
    - sa.DateTime with no timezone=True argument (matches all existing DateTime columns in this codebase)
  </behavior>
  <action>
    Create alembic/versions/007_billing_subscription_columns.py.

    File header (per D-03 and confirmed migration chain from RESEARCH.md Q2):
      revision = "007"
      down_revision = "006"
      branch_labels = None
      depends_on = None

    upgrade() — add columns to "dealerships" table using op.add_column():
      1. subscription_status: sa.String(32), nullable=True
      2. subscription_id: sa.String(128), nullable=True
      3. ls_customer_id: sa.String(128), nullable=True
      4. plan: sa.String(64), nullable=True
      5. trial_ends_at: sa.DateTime, nullable=True  (no timezone arg — matches codebase pattern)
      6. grace_period_ends_at: sa.DateTime, nullable=True

    downgrade() — drop columns in reverse order:
      grace_period_ends_at, trial_ends_at, plan, ls_customer_id, subscription_id, subscription_status

    Imports: import sqlalchemy as sa; from alembic import op
    Do NOT add alembic_version stamp comment block — match the minimal style of existing version files.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -c "import ast, sys; ast.parse(open('alembic/versions/007_billing_subscription_columns.py').read()); print('syntax ok')" && python -c "from alembic.script.revision import Revision; content=open('alembic/versions/007_billing_subscription_columns.py').read(); assert '\"007\"' in content or \"'007'\" in content; assert '\"006\"' in content or \"'006'\" in content; print('revision chain ok')"</automated>
  </verify>
  <done>File exists, parses without error, revision="007", down_revision="006", 6 op.add_column() calls and 6 op.drop_column() calls present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dealership model — add 6 subscription columns to ORM class</name>
  <files>src/db/models.py</files>
  <behavior>
    - Dealership class gains 6 new Column() attributes in the same order as the migration
    - Column names, types, and nullability match the migration exactly
    - No other models or existing columns are changed
    - The 6 new columns are placed between created_at and the relationships block
  </behavior>
  <action>
    Edit src/db/models.py. Inside the Dealership class, after the line:
      created_at = Column(DateTime, default=_utcnow)
    and before the line:
      inventory_items = relationship(...)

    Insert these 6 new columns (per D-02):
      subscription_status = Column(String(32), nullable=True)
      subscription_id = Column(String(128), nullable=True)
      ls_customer_id = Column(String(128), nullable=True)
      plan = Column(String(64), nullable=True)
      trial_ends_at = Column(DateTime, nullable=True)
      grace_period_ends_at = Column(DateTime, nullable=True)

    String and DateTime are already imported at the top of the file — no new imports needed.
    Do NOT add a blank line between created_at and the new columns block — match the style of existing columns.
    Do NOT modify any other class or any existing column.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -c "from src.db.models import Dealership; d = Dealership(); attrs = ['subscription_status','subscription_id','ls_customer_id','plan','trial_ends_at','grace_period_ends_at']; [getattr(d, a) for a in attrs]; print('all 6 columns accessible')"</automated>
  </verify>
  <done>Dealership can be instantiated; all 6 subscription attributes exist and default to None; existing attributes (id, name, whatsapp_phone_number_id, etc.) are unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/services/billing.py with is_subscription_active()</name>
  <files>src/services/billing.py</files>
  <behavior>
    - is_subscription_active(None) returns False (defensive — handles orphaned dealership lookup)
    - is_subscription_active(dealership with status="active") returns True (per D-16)
    - is_subscription_active(dealership with status="trial") returns True (per D-16)
    - is_subscription_active(dealership with status="past_due", grace_period_ends_at=now+3d) returns True (per D-13)
    - is_subscription_active(dealership with status="past_due", grace_period_ends_at=now-1d) returns False (per D-14)
    - is_subscription_active(dealership with status="past_due", grace_period_ends_at=None) returns False
    - is_subscription_active(dealership with status="cancelled") returns False
    - is_subscription_active(dealership with status="expired") returns False
    - is_subscription_active(dealership with status=None, trial_ends_at=now+5d) returns True (per D-19 exception)
    - is_subscription_active(dealership with status=None, trial_ends_at=None) returns False (per D-19)
    - is_subscription_active(dealership with status=None, trial_ends_at=now-1d) returns False
    - Naive datetime from DB (tzinfo=None) is normalized to UTC before comparison — no TypeError raised
    - map_ls_status("on_trial") returns "trial"
    - map_ls_status("paused") returns "past_due"
    - map_ls_status("unknown_future_value") returns "expired" (safe default)
    - LS_STATUS_MAP covers all 7 LS status values: on_trial, active, past_due, paused, unpaid, cancelled, expired
  </behavior>
  <action>
    Create src/services/billing.py. File has no I/O, no async, no DB imports. Pure Python only.

    Imports needed:
      from datetime import UTC, datetime

    LS_STATUS_MAP dict (per D-08):
      "on_trial"  -> "trial"
      "active"    -> "active"
      "past_due"  -> "past_due"
      "paused"    -> "past_due"
      "unpaid"    -> "past_due"
      "cancelled" -> "cancelled"
      "expired"   -> "expired"

    map_ls_status(ls_status: str) -> str:
      Returns LS_STATUS_MAP.get(ls_status, "expired")
      Safe default is "expired" (most restrictive) for unknown future LS values.

    is_subscription_active(dealership) -> bool:
      1. If dealership is None: return False
      2. status = dealership.subscription_status
      3. If status in ("active", "trial"): return True
      4. If status == "past_due":
           gpe = dealership.grace_period_ends_at
           if gpe is None: return False
           now = datetime.now(UTC)
           if gpe.tzinfo is None: gpe = gpe.replace(tzinfo=UTC)  # normalize naive DB datetime
           return now < gpe
      5. If status is None:  (D-19 exception: no subscription but trial_ends_at set)
           tea = dealership.trial_ends_at
           if tea is None: return False
           now = datetime.now(UTC)
           if tea.tzinfo is None: tea = tea.replace(tzinfo=UTC)  # normalize naive DB datetime
           return now < tea
      6. return False  (catches "cancelled", "expired", and any other value)

    The normalize pattern (`.replace(tzinfo=UTC)` when `tzinfo is None`) is critical:
    SQLAlchemy DateTime columns without timezone=True return naive datetimes.
    Comparing naive with aware raises TypeError. This codebase already uses this
    pattern in followup_task.py lines 145-147 — match it exactly.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -c "
from src.services.billing import is_subscription_active, map_ls_status, LS_STATUS_MAP
from datetime import datetime, UTC, timedelta

class FakeDealer:
    def __init__(self, **kw):
        self.subscription_status = kw.get('subscription_status')
        self.grace_period_ends_at = kw.get('grace_period_ends_at')
        self.trial_ends_at = kw.get('trial_ends_at')

now = datetime.now(UTC)
assert is_subscription_active(None) == False
assert is_subscription_active(FakeDealer(subscription_status='active')) == True
assert is_subscription_active(FakeDealer(subscription_status='trial')) == True
assert is_subscription_active(FakeDealer(subscription_status='past_due', grace_period_ends_at=now+timedelta(days=3))) == True
assert is_subscription_active(FakeDealer(subscription_status='past_due', grace_period_ends_at=now-timedelta(days=1))) == False
assert is_subscription_active(FakeDealer(subscription_status='past_due', grace_period_ends_at=None)) == False
assert is_subscription_active(FakeDealer(subscription_status='expired')) == False
assert is_subscription_active(FakeDealer(subscription_status='cancelled')) == False
assert is_subscription_active(FakeDealer(subscription_status=None, trial_ends_at=now+timedelta(days=5))) == True
assert is_subscription_active(FakeDealer(subscription_status=None, trial_ends_at=None)) == False

# Naive datetime normalization (SQLite/PostgreSQL without tz)
naive_future = (now + timedelta(days=3)).replace(tzinfo=None)
assert is_subscription_active(FakeDealer(subscription_status='past_due', grace_period_ends_at=naive_future)) == True

assert map_ls_status('on_trial') == 'trial'
assert map_ls_status('paused') == 'past_due'
assert map_ls_status('unpaid') == 'past_due'
assert map_ls_status('not_a_real_status') == 'expired'
assert len(LS_STATUS_MAP) == 7
print('all billing.py assertions passed')
"</automated>
  </verify>
  <done>All 14 behavioral assertions pass. Naive datetime normalization confirmed working. map_ls_status and LS_STATUS_MAP cover all 7 LS status values.</done>
</task>

</tasks>

<verification>
Run the three automated verify commands above sequentially.
Then run: cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && python -m pytest tests/ -x -q --tb=short 2>&1 | head -40
Existing tests must not regress (the 6 new nullable columns have None defaults, so existing fixtures are unaffected).
</verification>

<success_criteria>
- alembic/versions/007_billing_subscription_columns.py exists with revision="007", down_revision="006", 6 add_column calls, 6 drop_column calls
- src/db/models.py Dealership has 6 new nullable columns; existing model tests still pass
- src/services/billing.py passes all 14 inline assertions; no imports beyond datetime stdlib
- No existing test regressions
</success_criteria>

<output>
After completion, create .planning/phases/08-billing/08-01-SUMMARY.md with:
- Files created/modified
- Key decisions implemented (D-01 through D-03, D-08, D-16, D-17, D-19)
- The naive datetime normalization pattern used
- Exact column names and types for downstream plans to reference
</output>
