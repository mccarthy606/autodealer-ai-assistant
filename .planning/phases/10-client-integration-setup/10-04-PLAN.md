---
phase: 10-client-integration-setup
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/api/routes/admin_settings.py
  - src/templates/admin/integrations.html
autonomous: true
requirements:
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Admin integrations page shows form fields for WhatsApp credentials (4 fields) and ML credentials (5 fields) labeled in Spanish"
    - "Submitting the form with non-blank values saves those values to the Dealership row; blank fields are skipped (existing value preserved)"
    - "Connection status badges show Conectado/No configurado based on DB values, not settings"
    - "Verificar conexion button calls /admin/ui/integrations/test-connection and shows result inline without page reload"
    - "Test connection for WhatsApp calls graph.facebook.com/v18.0/{phone_id} and returns sanitized result"
    - "Test connection for MercadoLibre calls api.mercadolibre.com/users/me and returns sanitized result"
    - "Token fields are never pre-populated with actual token values — masked placeholder shown instead"
    - "WhatsApp activation checklist is visible as a collapsible section on the integrations page"
    - "ML linked cars table is preserved unchanged"
  artifacts:
    - path: "src/api/routes/admin_settings.py"
      provides: "Save integrations endpoint + test-connection endpoint"
      contains: "test-connection"
    - path: "src/templates/admin/integrations.html"
      provides: "Redesigned integrations page with Spanish forms"
      contains: "Verificar conexion"
  key_links:
    - from: "src/templates/admin/integrations.html"
      to: "src/api/routes/admin_settings.py"
      via: "POST /admin/ui/integrations form action"
      pattern: "action=./admin/ui/integrations."
    - from: "src/templates/admin/integrations.html"
      to: "/admin/ui/integrations/test-connection"
      via: "fetch() in inline JS"
      pattern: "test-connection"
---

<objective>
Add credential save and test-connection endpoints to admin_settings.py, and redesign integrations.html with Spanish forms replacing the old .env-instruction layout.

Purpose: INT-02 and INT-03 — dealership owner can enter WhatsApp and ML credentials through the admin UI and immediately verify the connection without editing .env or restarting Docker.

Output:
- src/api/routes/admin_settings.py: POST /integrations (save) + POST /integrations/test-connection
- src/templates/admin/integrations.html: full redesign with forms, status badges, test buttons, WA checklist
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-client-integration-setup/10-CONTEXT.md
@.planning/phases/10-client-integration-setup/10-RESEARCH.md
@.planning/phases/10-client-integration-setup/10-01-SUMMARY.md

<interfaces>
<!-- Current admin_settings.py structure (full file is ~86 lines) -->
```python
router = APIRouter(prefix="/admin/ui", tags=["admin-ui"])

# Existing routes:
GET  /admin/ui/settings        -> settings_page()
POST /admin/ui/settings        -> settings_save()
GET  /admin/ui/integrations    -> integrations_page()

# auth_check pattern:
did = await auth_check(request)
if not isinstance(did, int):
    return did

# DB query pattern:
stmt = select(Dealership).where(Dealership.id == did)
r = await db.execute(stmt)
dealer = r.scalar_one_or_none()

# Redirect-after-save pattern:
return RedirectResponse(url="/admin/ui/settings?saved=1", status_code=302)
```

<!-- New Dealership columns from plan 10-01 -->
dealer.whatsapp_webhook_secret   # String(128), nullable
dealer.ml_access_token           # String(512), nullable
dealer.ml_refresh_token          # String(512), nullable
dealer.ml_app_id                 # String(64), nullable
dealer.ml_client_secret          # String(128), nullable

<!-- Existing Dealership columns relevant to integrations -->
dealer.whatsapp_phone_number_id  # String(64)
dealer.whatsapp_access_token     # String(512), nullable
dealer.whatsapp_verify_token     # String(128)
dealer.ml_user_id                # String(64)

<!-- Test-connection WA API call -->
GET https://graph.facebook.com/v18.0/{phone_number_id}
Headers: Authorization: Bearer {token}
Success: resp.status_code == 200, return data.get("display_phone_number") or data.get("verified_name")
Failure: return data.get("error", {}).get("message", f"HTTP {resp.status_code}")

<!-- Test-connection ML API call -->
GET https://api.mercadolibre.com/users/me
Headers: Authorization: Bearer {token}
Success: resp.status_code == 200, return data.get("nickname") or str(data.get("id"))
Failure: "Token invalido — refrescar token ML"

<!-- Spanish field labels (from RESEARCH.md Pattern 5) -->
whatsapp_phone_number_id  -> "ID de numero de telefono"
whatsapp_access_token     -> "Token de acceso"
whatsapp_verify_token     -> "Token de verificacion"
whatsapp_webhook_secret   -> "Secreto del webhook"
ml_access_token           -> "Token de acceso ML"
ml_refresh_token          -> "Token de refresco ML"
ml_app_id                 -> "ID de aplicacion ML"
ml_client_secret          -> "Secreto de cliente ML"
ml_user_id                -> "ID de usuario ML"

<!-- WA Activation checklist content (from CONTEXT.md) -->
1. Abrir Meta for Developers → WhatsApp → API Setup
2. Verificar que el numero este en modo "Live" (no "Development")
3. En Development mode: agregar el numero del cliente al whitelist de prueba
4. Configurar el webhook URL: https://tu-dominio/webhooks/whatsapp_cloud
5. Verify Token debe coincidir con el campo guardado
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /integrations and POST /integrations/test-connection to admin_settings.py</name>
  <files>src/api/routes/admin_settings.py</files>
  <read_first>
    - src/api/routes/admin_settings.py (read full file — especially the imports block to see what is already imported)
    - src/config.py (confirm settings.whatsapp_cloud_token field name, settings.whatsapp_phone_number_id, settings.ml_access_token)
  </read_first>
  <action>
Add two new routes to the bottom of src/api/routes/admin_settings.py.

First, ensure httpx is imported at the top. If not already present, add:
```python
import httpx
```

Add the save route:
```python
@router.post("/integrations")
async def integrations_save(request: Request, db: AsyncSession = Depends(get_db)):
    """Save WhatsApp and MercadoLibre credentials to the Dealership row."""
    did = await auth_check(request)
    if not isinstance(did, int):
        return did

    form = await request.form()
    stmt = select(Dealership).where(Dealership.id == did)
    r = await db.execute(stmt)
    dealer = r.scalar_one_or_none()

    if dealer:
        # Only update non-blank submitted fields — blank = keep existing value
        if form.get("whatsapp_phone_number_id"):
            dealer.whatsapp_phone_number_id = form["whatsapp_phone_number_id"].strip()
        if form.get("whatsapp_access_token"):
            dealer.whatsapp_access_token = form["whatsapp_access_token"].strip()
        if form.get("whatsapp_verify_token"):
            dealer.whatsapp_verify_token = form["whatsapp_verify_token"].strip()
        if form.get("whatsapp_webhook_secret"):
            dealer.whatsapp_webhook_secret = form["whatsapp_webhook_secret"].strip()
        if form.get("ml_access_token"):
            dealer.ml_access_token = form["ml_access_token"].strip()
        if form.get("ml_refresh_token"):
            dealer.ml_refresh_token = form["ml_refresh_token"].strip()
        if form.get("ml_app_id"):
            dealer.ml_app_id = form["ml_app_id"].strip()
        if form.get("ml_client_secret"):
            dealer.ml_client_secret = form["ml_client_secret"].strip()
        if form.get("ml_user_id"):
            dealer.ml_user_id = form["ml_user_id"].strip()
        await db.commit()

    return RedirectResponse(url="/admin/ui/integrations?saved=1", status_code=302)
```

Add the test-connection route:
```python
@router.post("/integrations/test-connection")
async def test_connection(request: Request, db: AsyncSession = Depends(get_db)):
    """Live API validation for WhatsApp or MercadoLibre credentials."""
    did = await auth_check(request)
    if not isinstance(did, int):
        return {"ok": False, "detail": "No autenticado"}

    try:
        body = await request.json()
    except Exception:
        return {"ok": False, "detail": "Solicitud invalida"}

    service = body.get("service")  # "whatsapp" or "mercadolibre"

    stmt = select(Dealership).where(Dealership.id == did)
    r = await db.execute(stmt)
    dealer = r.scalar_one_or_none()
    if not dealer:
        return {"ok": False, "detail": "Concesionario no encontrado"}

    if service == "whatsapp":
        token = dealer.whatsapp_access_token or settings.whatsapp_cloud_token
        phone_id = dealer.whatsapp_phone_number_id or settings.whatsapp_phone_number_id
        if not token or not phone_id:
            return {"ok": False, "detail": "Credenciales no configuradas"}
        try:
            async with httpx.AsyncClient(timeout=10) as client:
                resp = await client.get(
                    f"https://graph.facebook.com/v18.0/{phone_id}",
                    headers={"Authorization": f"Bearer {token}"},
                )
            data = resp.json()
            if resp.status_code == 200:
                name = data.get("display_phone_number") or data.get("verified_name") or phone_id
                return {"ok": True, "detail": f"Conectado: {name}"}
            error = data.get("error", {}).get("message", f"HTTP {resp.status_code}")
            return {"ok": False, "detail": f"Error de Meta: {error}"}
        except Exception:
            return {"ok": False, "detail": "Error de red — verificar conexion a internet"}

    if service == "mercadolibre":
        token = dealer.ml_access_token or settings.ml_access_token
        if not token:
            return {"ok": False, "detail": "Token ML no configurado"}
        try:
            async with httpx.AsyncClient(timeout=10) as client:
                resp = await client.get(
                    "https://api.mercadolibre.com/users/me",
                    headers={"Authorization": f"Bearer {token}"},
                )
            data = resp.json()
            if resp.status_code == 200:
                nickname = data.get("nickname") or str(data.get("id", ""))
                return {"ok": True, "detail": f"Conectado como: {nickname}"}
            return {"ok": False, "detail": "Token invalido — refrescar token ML"}
        except Exception:
            return {"ok": False, "detail": "Error de red — verificar conexion a internet"}

    return {"ok": False, "detail": "Servicio desconocido"}
```

Do not modify the existing settings_page, settings_save, or integrations_page functions.
  </action>
  <verify>
    <automated>grep -n "integrations_save\|test_connection\|test-connection" /c/Users/user/OneDrive/Desktop/autodealer-ai-assistant/src/api/routes/admin_settings.py</automated>
  </verify>
  <acceptance_criteria>
    - Function integrations_save() is defined with @router.post("/integrations")
    - Function test_connection() is defined with @router.post("/integrations/test-connection")
    - integrations_save checks form.get("whatsapp_access_token") before assigning (blank-skip pattern)
    - integrations_save calls await db.commit() after assignments
    - integrations_save returns RedirectResponse to /admin/ui/integrations?saved=1
    - test_connection returns {"ok": True/False, "detail": "..."} JSON dict
    - test_connection contains "graph.facebook.com/v18.0/" for WhatsApp
    - test_connection contains "api.mercadolibre.com/users/me" for ML
    - Neither endpoint returns raw token values in any response
    - Python syntax valid: python -c "import ast; ast.parse(open('src/api/routes/admin_settings.py').read()); print('OK')"
  </acceptance_criteria>
  <done>POST /integrations saves credentials (blank-skip); POST /integrations/test-connection validates live API calls; both auth-gated</done>
</task>

<task type="auto">
  <name>Task 2: Redesign integrations.html with Spanish credential forms</name>
  <files>src/templates/admin/integrations.html</files>
  <read_first>
    - src/templates/admin/integrations.html (read entire current template to understand structure, existing ml_cars loop, and base template extension)
    - src/templates/admin/settings.html (read for form and input CSS class patterns used in this project)
  </read_first>
  <action>
Rewrite src/templates/admin/integrations.html completely. Preserve:
- {% extends "base.html" %} and {% block %} structure
- The ML linked cars table ({% if ml_cars %} ... {% for car in ml_cars %} loop)
- The ML import-by-URL form (POST /admin/ui/cars/import-ml-url)

Replace:
- The .env snippet instructions for both WhatsApp and ML
- Static status display with editable form fields

New template context variables needed from GET /integrations route (update integrations_page() in admin_settings.py after this task to pass them):
- dealer (Dealership object)
- wa_configured (bool — True if dealer.whatsapp_access_token or settings.whatsapp_cloud_token)
- ml_configured (bool — True if dealer.ml_access_token or settings.ml_access_token)
- ml_cars (list)
- saved (query param)

Write the template as follows:

```html
{% extends "base.html" %}
{% block title %}Integraciones{% endblock %}
{% block content %}
<div class="page-header">
    <h1>Integraciones</h1>
    {% if saved %}<div class="alert alert-success">Credenciales guardadas.</div>{% endif %}
</div>

<form method="post" action="/admin/ui/integrations">
<div class="integrations-grid">

    <!-- WhatsApp Business Cloud -->
    <div class="card integration-card">
        <div class="integration-header">
            <h3>WhatsApp Business Cloud</h3>
            <span class="badge {% if wa_configured %}badge-connected{% else %}badge-disconnected{% endif %}">
                {{ 'Conectado' if wa_configured else 'No configurado' }}
            </span>
        </div>

        <div class="form-group">
            <label>ID de numero de telefono</label>
            <input type="text" name="whatsapp_phone_number_id"
                   value="{{ dealer.whatsapp_phone_number_id or '' }}"
                   placeholder="Ej: 1234567890">
        </div>
        <div class="form-group">
            <label>Token de acceso</label>
            <input type="password" name="whatsapp_access_token"
                   value=""
                   placeholder="{{ '(token guardado — dejar en blanco para no cambiar)' if wa_configured else 'Pegar token aqui' }}">
        </div>
        <div class="form-group">
            <label>Token de verificacion</label>
            <input type="text" name="whatsapp_verify_token"
                   value=""
                   placeholder="{{ '(guardado)' if dealer and dealer.whatsapp_verify_token else 'Crear un token secreto' }}">
        </div>
        <div class="form-group">
            <label>Secreto del webhook</label>
            <input type="password" name="whatsapp_webhook_secret"
                   value=""
                   placeholder="{{ '(guardado)' if dealer and dealer.whatsapp_webhook_secret else 'App Secret de Meta (opcional)' }}">
        </div>
        <div class="form-group">
            <span class="hint">URL del webhook: <code>/webhooks/whatsapp_cloud</code></span>
        </div>

        <div style="display:flex;gap:8px;align-items:center;margin-top:12px;">
            <button type="button" class="btn btn-secondary"
                    onclick="testConnection('whatsapp', this)">Verificar conexion</button>
            <span id="wa-test-result" style="font-size:14px;"></span>
        </div>

        <!-- Checklist de activacion WhatsApp (collapsible) -->
        <details style="margin-top:16px;">
            <summary style="cursor:pointer;font-weight:600;">Checklist de activacion WhatsApp</summary>
            <ol style="margin-top:8px;padding-left:20px;line-height:1.8;">
                <li>Abrir Meta for Developers &rarr; WhatsApp &rarr; API Setup</li>
                <li>Verificar que el numero este en modo &ldquo;Live&rdquo; (no &ldquo;Development&rdquo;)</li>
                <li>En Development mode: agregar el numero del cliente al whitelist de prueba</li>
                <li>Configurar el webhook URL: <code>https://tu-dominio/webhooks/whatsapp_cloud</code></li>
                <li>El Verify Token debe coincidir con el campo guardado arriba</li>
            </ol>
        </details>
    </div>

    <!-- MercadoLibre -->
    <div class="card integration-card">
        <div class="integration-header">
            <h3>MercadoLibre</h3>
            <span class="badge {% if ml_configured %}badge-connected{% else %}badge-disconnected{% endif %}">
                {{ 'Conectado' if ml_configured else 'No configurado' }}
            </span>
        </div>

        <div class="form-group">
            <label>ID de usuario ML</label>
            <input type="text" name="ml_user_id"
                   value="{{ dealer.ml_user_id or '' }}"
                   placeholder="Tu seller ID de MercadoLibre">
        </div>
        <div class="form-group">
            <label>Token de acceso ML</label>
            <input type="password" name="ml_access_token"
                   value=""
                   placeholder="{{ '(token guardado — dejar en blanco para no cambiar)' if ml_configured else 'Pegar access_token aqui' }}">
        </div>
        <div class="form-group">
            <label>Token de refresco ML</label>
            <input type="password" name="ml_refresh_token"
                   value=""
                   placeholder="{{ '(guardado)' if dealer and dealer.ml_refresh_token else 'Pegar refresh_token aqui' }}">
        </div>
        <div class="form-group">
            <label>ID de aplicacion ML</label>
            <input type="text" name="ml_app_id"
                   value="{{ dealer.ml_app_id or '' }}"
                   placeholder="App ID de tu aplicacion ML">
        </div>
        <div class="form-group">
            <label>Secreto de cliente ML</label>
            <input type="password" name="ml_client_secret"
                   value=""
                   placeholder="{{ '(guardado)' if dealer and dealer.ml_client_secret else 'Client Secret de tu app ML' }}">
        </div>

        <div style="display:flex;gap:8px;align-items:center;margin-top:12px;">
            <button type="button" class="btn btn-secondary"
                    onclick="testConnection('mercadolibre', this)">Verificar conexion</button>
            <span id="ml-test-result" style="font-size:14px;"></span>
        </div>

        <!-- ML linked cars — preserved as-is -->
        {% if ml_cars %}
        <hr style="margin: 16px 0; border: none; border-top: 1px solid var(--border);">
        <h4>Listings vinculados a ML ({{ ml_cars|length }})</h4>
        <table class="compact-table">
            <thead><tr><th>Auto</th><th>ML Item ID</th><th>Fotos</th><th>Precio</th></tr></thead>
            <tbody>
                {% for car in ml_cars %}
                <tr>
                    <td><a href="/admin/ui/cars/{{ car.id }}">{{ car.brand }} {{ car.model }} {{ car.year if car.year else '' }}</a></td>
                    <td><code>{{ car.ml_item_id }}</code></td>
                    <td>{{ car.photos|length if car.photos else 0 }}</td>
                    <td>${{ "{:,.0f}".format(car.price) }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}

        <!-- ML import by URL — preserved as-is -->
        <div class="ml-import-section" style="margin-top:16px;">
            <h4>Importar auto por link ML</h4>
            <form method="post" action="/admin/ui/cars/import-ml-url" class="import-form">
                <div style="display:flex;gap:12px;align-items:flex-end;">
                    <div class="form-group" style="flex:1">
                        <input type="text" name="ml_url" placeholder="https://auto.mercadolibre.com.ar/MLA-..." style="font-size:15px;">
                    </div>
                    <button type="submit" class="btn btn-primary">Importar</button>
                </div>
            </form>
        </div>
    </div>

</div><!-- end integrations-grid -->

<div style="margin-top:20px;">
    <button type="submit" class="btn btn-primary">Guardar credenciales</button>
</div>
</form>

<script>
async function testConnection(service, btn) {
    const resultId = service === 'whatsapp' ? 'wa-test-result' : 'ml-test-result';
    const el = document.getElementById(resultId);
    el.textContent = 'Verificando...';
    btn.disabled = true;
    try {
        const r = await fetch('/admin/ui/integrations/test-connection', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({service: service})
        });
        const data = await r.json();
        el.textContent = data.ok ? '\u2705 ' + data.detail : '\u274c ' + data.detail;
        el.style.color = data.ok ? 'var(--success, green)' : 'var(--danger, red)';
    } catch (e) {
        el.textContent = '\u274c Error de red';
        el.style.color = 'var(--danger, red)';
    } finally {
        btn.disabled = false;
    }
}
</script>
{% endblock %}
```

Also update the GET integrations_page() function in admin_settings.py to pass `dealer` and `saved` to the template context, and compute wa_configured / ml_configured from the dealer DB row (not just settings):

```python
@router.get("/integrations", response_class=HTMLResponse)
async def integrations_page(request: Request, db: AsyncSession = Depends(get_db)):
    did = await auth_check(request)
    if not isinstance(did, int):
        return did

    stmt = select(Dealership).where(Dealership.id == did)
    r = await db.execute(stmt)
    dealer = r.scalar_one_or_none()

    wa_configured = bool(
        (dealer and dealer.whatsapp_access_token) or settings.whatsapp_cloud_token
    )
    ml_configured = bool(
        (dealer and dealer.ml_access_token) or settings.ml_access_token
    )

    stmt2 = select(InventoryItem).where(
        InventoryItem.dealership_id == did,
        InventoryItem.ml_item_id.isnot(None),
    )
    r2 = await db.execute(stmt2)
    ml_cars = r2.scalars().all()

    saved = request.query_params.get("saved")

    return templates.TemplateResponse("admin/integrations.html", {
        "request": request,
        "dealer": dealer,
        "wa_configured": wa_configured,
        "ml_configured": ml_configured,
        "ml_cars": ml_cars,
        "saved": saved,
    })
```
  </action>
  <verify>
    <automated>grep -n "Verificar conexion\|test-connection\|whatsapp_access_token\|ml_access_token\|Checklist" /c/Users/user/OneDrive/Desktop/autodealer-ai-assistant/src/templates/admin/integrations.html</automated>
  </verify>
  <acceptance_criteria>
    - Template contains "Verificar conexion" button for WhatsApp
    - Template contains "Verificar conexion" button for MercadoLibre
    - Template contains fetch('/admin/ui/integrations/test-connection') in the script block
    - Template contains "Checklist de activacion WhatsApp" (the collapsible section)
    - Template contains all 4 WhatsApp form fields: whatsapp_phone_number_id, whatsapp_access_token, whatsapp_verify_token, whatsapp_webhook_secret
    - Template contains all 5 ML form fields: ml_user_id, ml_access_token, ml_refresh_token, ml_app_id, ml_client_secret
    - Token inputs (whatsapp_access_token, ml_access_token, ml_refresh_token, whatsapp_webhook_secret, ml_client_secret) use type="password" with blank value="" and non-empty placeholder
    - Non-sensitive fields (whatsapp_phone_number_id, ml_user_id, ml_app_id) use type="text" with value="{{ dealer.field or '' }}"
    - ML linked cars {% for car in ml_cars %} loop is present
    - ML import-by-URL form action="/admin/ui/cars/import-ml-url" is present
    - admin_settings.py integrations_page() passes "dealer" key to template context
    - admin_settings.py integrations_page() passes "saved" key to template context
    - admin_settings.py wa_configured computation uses dealer.whatsapp_access_token (not only settings)
  </acceptance_criteria>
  <done>Integrations page shows Spanish forms; credentials save with blank-skip; Verificar conexion shows inline result; WA checklist visible; ML linked cars preserved</done>
</task>

</tasks>

<verification>
After both tasks:
1. grep -c "test-connection" src/templates/admin/integrations.html src/api/routes/admin_settings.py
2. python -c "import ast; ast.parse(open('src/api/routes/admin_settings.py').read()); print('OK')"
3. grep "type=\"password\"" src/templates/admin/integrations.html — must return lines for all token fields
4. grep "value=\"\"" src/templates/admin/integrations.html — token inputs must have blank value (not actual token)
5. grep "import-ml-url" src/templates/admin/integrations.html — ML import form preserved
</verification>

<success_criteria>
- POST /integrations saves non-blank credential fields to Dealership row; blank fields skip update
- POST /integrations/test-connection returns {"ok": bool, "detail": str} for both services
- integrations.html shows form with 9 credential fields labeled in Spanish
- Token fields use type="password" with empty value (tokens never rendered to HTML)
- Verificar conexion JS calls test-connection and updates inline span without page reload
- WA checklist rendered in collapsible details element
- ML linked cars table and import form unchanged
</success_criteria>

<output>
After completion, create .planning/phases/10-client-integration-setup/10-04-SUMMARY.md
</output>
