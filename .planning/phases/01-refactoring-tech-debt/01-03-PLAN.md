---
phase: 01-refactoring-tech-debt
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/db/models.py
  - src/services/conversation_engine.py
  - src/services/lead_service.py
  - src/api/routes/admin.py
  - src/api/routes/admin_dashboard.py
  - src/api/routes/admin_inventory.py
  - src/api/routes/admin_conversations.py
  - src/api/routes/admin_settings.py
autonomous: true
requirements: [REF-03]

must_haves:
  truths:
    - "Zero occurrences of datetime.utcnow() in the entire src/ directory"
    - "All datetime.now(UTC) calls use the correct import (from datetime import datetime, UTC)"
    - "SQLAlchemy model defaults use lambda or helper function, not bare datetime.now(UTC)"
    - "All existing tests still pass after the migration"
  artifacts:
    - path: "src/db/models.py"
      provides: "Model defaults using _utcnow helper instead of datetime.utcnow"
      contains: "_utcnow"
  key_links:
    - from: "src/db/models.py"
      to: "datetime.UTC"
      via: "_utcnow helper function"
      pattern: "def _utcnow"
---

<objective>
Replace all datetime.utcnow() calls with datetime.now(UTC) across the entire codebase.

Purpose: REF-03 requires eliminating the deprecated datetime.utcnow() API. Research identified 25 total occurrences across 6 files (but orchestrator.py is already deleted by Plan 01). After Plans 01 and 02 complete, the remaining files are: models.py (9 model defaults), conversation_engine.py (3 call sites), admin routes (7 call sites now split across modules), admin.py (1), lead_service.py (1).

Output: Zero datetime.utcnow() in codebase, all using datetime.now(UTC) with proper imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactoring-tech-debt/01-CONTEXT.md
@.planning/phases/01-refactoring-tech-debt/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix model defaults in models.py with _utcnow helper</name>
  <files>src/db/models.py</files>
  <read_first>
    - src/db/models.py (FULL read - must see every Column default and onupdate that references datetime.utcnow)
  </read_first>
  <action>
Per D-09:

1. At the top of `src/db/models.py`, update the datetime import:
```python
# Before:
from datetime import datetime
# After:
from datetime import datetime, UTC
```

2. Add a module-level helper function right after imports:
```python
def _utcnow():
    """Return current UTC time using non-deprecated API."""
    return datetime.now(UTC)
```

3. Replace ALL 9 model default callables. The pattern change is:
```python
# Before:
created_at = Column(DateTime, default=datetime.utcnow)
updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

# After:
created_at = Column(DateTime, default=_utcnow)
updated_at = Column(DateTime, default=_utcnow, onupdate=_utcnow)
```

CRITICAL: Do NOT use `default=datetime.now(UTC)` (with parentheses) — that evaluates once at import time. Use `default=_utcnow` (no parentheses) to pass the callable.

Apply to all models: InventoryItem (created_at), Conversation (last_message_at, created_at, updated_at), Message (created_at), Lead (created_at), Event (created_at).

4. Verify with: `grep -n "datetime.utcnow" src/db/models.py` — must return zero matches.
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && grep -c "datetime.utcnow" src/db/models.py; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "datetime.utcnow" src/db/models.py` returns zero matches
    - `grep "def _utcnow" src/db/models.py` returns exactly 1 match
    - `grep "from datetime import datetime, UTC" src/db/models.py` returns 1 match
    - `grep "default=_utcnow" src/db/models.py` returns at least 5 matches (one per model with created_at)
    - No occurrence of `default=datetime.now(UTC)` with parentheses (would be a bug)
  </acceptance_criteria>
  <done>All 9 model datetime defaults use _utcnow helper. No deprecated datetime.utcnow in models.py.</done>
</task>

<task type="auto">
  <name>Task 2: Replace datetime.utcnow() call sites in all remaining source files and run full test suite</name>
  <files>src/services/conversation_engine.py, src/services/lead_service.py, src/api/routes/admin.py, src/api/routes/admin_dashboard.py, src/api/routes/admin_inventory.py, src/api/routes/admin_conversations.py, src/api/routes/admin_settings.py</files>
  <read_first>
    - src/services/conversation_engine.py (3 call sites at lines ~107, ~398, ~490)
    - src/services/lead_service.py (1 call site at line ~41)
    - src/api/routes/admin.py (1 call site at line ~198)
    - src/api/routes/admin_dashboard.py (check for datetime.utcnow from original admin_ui.py lines 89)
    - src/api/routes/admin_inventory.py (check for datetime.utcnow from original admin_ui.py lines 343, 358, 518, 539, 743)
    - src/api/routes/admin_conversations.py (check for datetime.utcnow)
    - src/api/routes/admin_settings.py (check for datetime.utcnow from original admin_ui.py line 822)
  </read_first>
  <action>
Per D-09:

For EACH file that contains `datetime.utcnow()`:

1. Update the import at the top of the file:
```python
# Before:
from datetime import datetime
# After:
from datetime import datetime, UTC
```

2. Replace every `datetime.utcnow()` call with `datetime.now(UTC)`:
```python
# Before:
conv.last_message_at = datetime.utcnow()
# After:
conv.last_message_at = datetime.now(UTC)
```

Files to check and fix (line numbers are approximate — READ each file first):
- `src/services/conversation_engine.py` — 3 occurrences (last_message_at, last_handoff_at assignments)
- `src/services/lead_service.py` — 1 occurrence (idempotency cutoff)
- `src/api/routes/admin.py` — 1 occurrence (metrics date calculation)
- `src/api/routes/admin_dashboard.py` — check for occurrences from original dashboard/metrics code
- `src/api/routes/admin_inventory.py` — check for occurrences from car CRUD timestamp updates
- `src/api/routes/admin_conversations.py` — check for occurrences
- `src/api/routes/admin_settings.py` — check for occurrences

3. After all replacements, run a comprehensive grep to confirm ZERO remaining occurrences:
```bash
grep -rn "datetime.utcnow" src/
```
This MUST return zero results.

4. Also check test files for datetime.utcnow usage:
```bash
grep -rn "datetime.utcnow" tests/
```
Fix any found in tests as well.

5. Run the full test suite: `docker compose run --rm api pytest tests/ -x -v`

6. Run the app import smoke test: `docker compose run --rm api python -c "from src.main import app; print('OK')"`
  </action>
  <verify>
    <automated>cd C:/Users/user/OneDrive/Desktop/autodealer-ai-assistant && grep -rc "datetime.utcnow" src/ tests/ && echo "FAIL: utcnow still found" || echo "PASS: no utcnow" && docker compose run --rm api pytest tests/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "datetime.utcnow" src/` returns zero matches
    - `grep -r "datetime.utcnow" tests/` returns zero matches
    - `grep "from datetime import datetime, UTC" src/services/conversation_engine.py` returns 1 match
    - `grep "from datetime import datetime, UTC" src/services/lead_service.py` returns 1 match
    - `grep "datetime.now(UTC)" src/services/conversation_engine.py` returns at least 1 match
    - `docker compose run --rm api pytest tests/ -x -v` exits with code 0
    - `docker compose run --rm api python -c "from src.main import app"` exits with code 0
  </acceptance_criteria>
  <done>Zero datetime.utcnow() in entire codebase. All files use datetime.now(UTC). All tests pass. REF-03 fully satisfied.</done>
</task>

</tasks>

<verification>
- `grep -r "datetime.utcnow" src/ tests/` returns absolutely nothing
- `grep -r "datetime.now(UTC)" src/` returns matches in all files that previously used utcnow
- `grep "def _utcnow" src/db/models.py` returns the helper function
- `docker compose run --rm api pytest tests/ -v` all green
- `docker compose run --rm api python -c "from src.main import app"` succeeds
</verification>

<success_criteria>
- Zero occurrences of deprecated datetime.utcnow() anywhere in src/ or tests/ (REF-03 satisfied)
- All model defaults use _utcnow callable helper (not evaluated-at-import)
- All call sites use datetime.now(UTC) with proper import
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-refactoring-tech-debt/01-03-SUMMARY.md`
</output>
